This is the .npmrc file in home directory, configuring the default repo npm uses to registry.npm.taobao.org

The .npmrc file should be like this:
#.npmrc

registry = 'https://registry.npm.taobao.org'

or ypu can change the global registry by running this command:

$npm config set registry https://registry.npm.taobao.org
